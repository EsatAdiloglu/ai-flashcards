import Box from '@mui/material/Box';
import Stack from '@mui/material/Stack';

type Options = {
    cards: Object[]     // TODO: Make better type
}

export default function CardGrid() {
    return (
        <Box>
            <Stack spacing={3}>

            </Stack>
        </Box>
    );
}